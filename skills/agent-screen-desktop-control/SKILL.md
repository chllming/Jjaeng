---
name: agent-screen-desktop-control
description: Optional high-risk desktop control workflow; disabled by default.
---

# Agent Screen desktop control

This skill is intentionally opt-in. It may eventually cover pointer, keyboard,
workspace switching, and window movement, but it must not be enabled by the
default Agent Screen MCP profile. Every mutating action requires explicit user
confirmation and must be scoped to a named window or workspace.
