name: release-binary

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-upload:
    name: Build and upload binary (x86_64)
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install --yes --no-install-recommends \
            libgtk-4-dev pkg-config cmake clang

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Restore Rust cache
        uses: Swatinem/rust-cache@v2

      - name: Stage user service for the binary archive
        run: cp packaging/jjaengd.service jjaengd.service

      - name: Build and upload binary
        uses: taiki-e/upload-rust-binary-action@v1
        with:
          bin: jjaeng,jjaengd,jjaeng-mcp,agent-screen
          archive: jjaeng-$target
          target: x86_64-unknown-linux-gnu
          include: jjaengd.service
          checksum: sha256
          token: ${{ secrets.GITHUB_TOKEN }}
